import { WithLeftMenu } from '@jssamples/ui';
import { Headers as LibHeaders } from '@jssamples/ui';

export default function ReactHeaders() {
  return (
    <WithLeftMenu>
      <LibHeaders />
    </WithLeftMenu>
  );
}
